# SiteFlow AI — PostgreSQL Database Directory

This directory contains PostgreSQL DDL scripts for SiteFlow AI platform.

## Setup Instructions

```bash
psql -U postgres -d postgres -f schema.sql
psql -U postgres -d postgres -f seed.sql
```
